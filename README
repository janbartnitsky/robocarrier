maps folder contains maps used in the contest tests. file name is a map dimension. map 1000 is used in tests 6 to 10.

tests.csv contains data for tests creation.

rand_test.py simulates test with given input parameters like intests.csv